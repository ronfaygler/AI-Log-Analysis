# MongoDB
MONGO_ROOT_USERNAME=logsentinel
MONGO_ROOT_PASSWORD=changeme
MONGO_DATABASE=logsentinel

# Redis
REDIS_PASSWORD=changeme

# JWT & API
JWT_SECRET=changeme-generate-a-long-random-string
JWT_EXPIRES_IN=7d
API_KEY_SALT=changeme
REDIS_QUEUE_NAME=logsentinel:jobs
CORS_ORIGIN=http://localhost:3000
COOKIE_SECURE=false

# Worker / Claude
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-latest #claude-sonnet-4-20250514
NOTIFY_WEBHOOK_URL=

# Service ports (host mapping)
API_PORT=4000
WORKER_PORT=
MCP_PORT=4001
FRONTEND_PORT=3000

# MCP server (dashboard credentials or a JWT from login)
MCP_EMAIL=
MCP_PASSWORD=
# MCP_AUTH_TOKEN=
